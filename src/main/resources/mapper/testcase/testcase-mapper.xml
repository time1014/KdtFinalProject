<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weple.cloud.testcase.mapper.TestCaseMapper">
	
	<select id="testCaseTaskList" resultType="TaskParentVO" ><!-- 일감등록 현재 선택된 프로젝트에 일감들의 목록 조회 -->
		SELECT 
			task_id AS taskId,
			task_title AS taskTitle
		FROM task
		WHERE project_id = #{pId}
	</select>
	
	<select id="coverageStatus" resultType="CoverdStatusVO">
		SELECT common_id,default_describe
		FROM common_code
		WHERE common_code = '0D'
	</select>
	
	<select id="testCaseMembers" resultType="TestCaseMemberVO"> <!-- 테스트케이스 현재 선택된 프로젝트 참여자 목록 조회 -->
		SELECT 
			u.user_code AS userCode,
			u.email AS email,
			u.user_name AS userName
		FROM members m
			INNER JOIN users u ON m.user_code = u.user_code
		WHERE m.project_id = #{pId} 
	</select>
	
	<select id="testCasePriorities" resultType="TestCasePriorityVO"><!-- 테스트 케이스 등록 등록된 우선순위 목록 -->
		SELECT 
			task_priority_id AS priorityId,
    		priority_name AS priorityName
		FROM task_priority
		WHERE company_id  = #{cId}  AND using_yn = 'Y'
	</select>

</mapper>