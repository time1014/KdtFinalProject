<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weple.cloud.system.mapper.SystemModuleMapper">
	 <select id="findModuleAll" resultType="SystemModuleVO">
	    SELECT common_id
	           , common_code
	           , default_describe
	    FROM common_code
	    WHERE common_code = '0B'
	    ORDER BY common_id
	</select>
    
    <!-- 회사가 이미 노출 설정해둔 모듈 코드 목록 -->
    <select id="findEnabledModuleCodes" parameterType="Long" resultType="String">
	    SELECT module_name
	    FROM modules
	    WHERE company_id = #{companyId}
	</select>

    <delete id="deleteModulesByCompany" parameterType="Long">
        DELETE FROM modules
        WHERE company_id = #{companyId}
    </delete>

    <insert id="insertModule">
        INSERT INTO modules (module_id, module_name, company_id)
        VALUES (#{moduleId}, #{moduleName}, #{companyId})
    </insert>
    
    <select id="findTaskTypeAll" parameterType="Long" resultType="TaskTypeVO">
	    SELECT type_id, type_name, task_type_describe
	    FROM task_types
	    WHERE company_id = #{companyId}
	</select>

	<!-- 회사가 새 프로젝트에 기본 포함하기로 저장해둔 일감유형 ID 목록 -->
	<!-- task_types가 이미 company_id로 회사에 종속돼 있어서, 별도 매핑 테이블 없이 이 컬럼 하나로 처리 -->
	<select id="findEnabledTaskTypeIds" parameterType="Long" resultType="String">
	    SELECT type_id
	    FROM task_types
	    WHERE company_id = #{companyId}
	      AND default_enabled_yn = 'Y'
	</select>

	<!-- 저장 전 초기화: 이 회사의 일감유형을 전부 '기본 미포함'으로 되돌림 -->
	<update id="resetTaskTypeEnabled" parameterType="Long">
	    UPDATE task_types
	    SET default_enabled_yn = 'N'
	    WHERE company_id = #{companyId}
	</update>

	<!-- 선택된 일감유형들만 '기본 포함'으로 표시 -->
	<update id="enableTaskTypes">
	    UPDATE task_types
	    SET default_enabled_yn = 'Y'
	    WHERE company_id = #{companyId}
	      AND type_id IN
	      <foreach collection="typeIds" item="typeId" open="(" separator="," close=")">
	          #{typeId}
	      </foreach>
	</update>
	
</mapper>