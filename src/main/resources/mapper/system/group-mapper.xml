<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weple.cloud.system.mapper.GroupMapper">
	<!--/* 그룹 종류 */-->
	<!--/* 전체조회 */-->
	<select id="selectGroupAll" resultType="SystemGroupVO">
		SELECT g.group_id,
       		   g.company_id,
       		   g.group_name,
       		   g.created_at,
       		   				(SELECT COUNT(*)
              	 			 FROM users u
                			 WHERE u.group_id = g.group_id
                			) AS user_count
		FROM groups g
		
		<where>
			g.company_id = #{companyId}
        	<if test="keyword != null and keyword != ''">
            	AND g.group_name LIKE '%' || #{keyword} || '%'
        	</if>
   		 </where>
   		 
		ORDER BY g.group_id ASC
	</select>
	
	<!--/* 등록 */-->
	<insert id="insertGroup" parameterType="SystemGroupVO">
		<selectKey keyProperty="groupId"
				   resultType="Integer"
				   order="BEFORE" >
			SELECT NVL(MAX(group_id), 0) + 10 FROM groups
		</selectKey>
		INSERT INTO groups
						  (
						  	group_id
						  	, company_id
						  	, group_name
						  	, created_at
						  )
		VALUES
			  (
			  	#{groupId}
			  	, #{companyId}
			  	, #{groupName}
			  	, SYSDATE
			  )
	</insert>
	
	<!-- 수정 -->
	<update id="updateGroup" parameterType="SystemGroupVO">
		UPDATE groups
		SET group_name = #{groupName}
		WHERE group_id = #{groupId}
	</update>

	<!-- 삭제 -->
	<delete id="deleteGroup" parameterType="int">
		DELETE FROM groups
		WHERE group_id = #{groupId}
	</delete>
	
	
	
</mapper>