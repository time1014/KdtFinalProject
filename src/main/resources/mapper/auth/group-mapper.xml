<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weple.cloud.system.mapper.SystemGroupMapper">
	<!-- 전체조회 -->
	
	<!-- 상세조회 -->
	
	<!-- 등록 -->
	<insert id="insertGroup" parameterType="SystemGroupVO">
		<selectKey keyProperty="groupId"
				   resultType="Integer"
				   order="BEFORE" >
			SELECT NVL(MAX(group_id), 0) + 10 FROM groups
		</selectKey>
		INSERT INTO groups
						  (
						  	group_id
						  	, company_id
						  	, group_name
						  )
		VALUES
			  (
			  	#{groupId}
			  	, #{companyId}
			  	, #{groupName}
			  )
	</insert>
	
	<!-- 수정 -->
	
	<!-- 삭제 -->
</mapper>