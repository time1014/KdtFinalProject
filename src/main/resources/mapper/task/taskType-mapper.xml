<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weple.cloud.system.mapper.SystemMapper">
	<select id="selectTaskTypeAll" resultType="TaskTypeVO">
		SELECT type_id
		     , company_id
		     , type_name
		     , task_type_describe
		     , position
		FROM task_types
		ORDER BY position
	</select>

 
	<insert id="insertTaskType" parameterType="TaskTypeVO">
    <selectKey resultType="TaskTypeVO" keyProperty="typeId,position" order="BEFORE">
        SELECT 
            NVL(MAX(type_id), 0) + 1 AS typeId,
            NVL(MAX(CASE WHEN company_id = #{companyId} THEN position END), 0) + 1 AS position
        FROM task_types
    </selectKey>

    INSERT INTO task_types (
        type_id,
        company_id,
        type_name,
        task_type_describe,
        position
    ) 
    VALUES (
        #{typeId}
        , #{companyId}
        , #{typeName}
        , #{taskTypeDescribe}
        , #{position}
    )
	</insert>
	
	<update id="updatePosition">
		UPDATE task_types
		SET position = #{position}
		WHERE type_id = #{typeId}
	</update>
	
	<update id="">
		UPDATE task_types
		SET 	type_name = #{typeName}
		  	  , task_type_describe = #{taskTypeDescribe}
		WHERE type_id = #{typeId}
	</update>
	
	
	
</mapper>