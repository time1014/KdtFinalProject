<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weple.cloud.outline.mapper.OutlineMapper">

	<select id="selectProjectById" resultType="ProjectVO">
	    SELECT 
	        project_id         AS projectId,
	        project_title      AS projectTitle,
	        project_describe   AS projectDescribe,
	        project_identifier AS projectIdentifier,
	        created_at         AS createdAt,
	        finish_date        AS finishDate,
	        status             AS status
	    FROM project
	    WHERE project_id = #{projectId}
	</select>


	<select id="selectProjectMembersByGroup" resultType="ProjectGroupMemberDTO">
    SELECT 
        g.group_name AS groupName,
        COUNT(m.member_id) AS memberCount,
        LISTAGG(u.user_name, ', ') WITHIN GROUP (ORDER BY u.user_name) AS memberNames
    FROM members m
    JOIN users u ON m.user_code = u.user_code
    LEFT JOIN groups g ON u.group_id = g.group_id
    WHERE m.project_id = #{projectId}
    GROUP BY g.group_name
    ORDER BY g.group_name ASC
</select>

<select id="selectRawTaskDetails" resultType="RawTaskDTO">
    SELECT 
        t.task_status              AS taskStatus,
        t.priority                 AS priority,
        tp.type_name               AS typeName,
        NVL(u.user_name, '미지정')  AS managerName,
        NVL(t.estimated_time, 0)   AS estimatedTime,
        NVL(t.spent_hours_sum, 0)  AS spentHoursSum
    FROM task t
    LEFT JOIN task_types tp ON t.type_id = tp.type_id
    LEFT JOIN users u ON t.task_manager = u.user_code
    WHERE t.project_id = #{projectId} AND t.deleted_yn = 'N'
</select>

<select id="isModuleActive" parameterType="map" resultType="int">
        SELECT COUNT(*)
        FROM module_mapping
        WHERE project_id = #{projectId}
          AND module_name = #{moduleName}
          AND is_active = 'Y'
    </select>
    
    <select id="checkProjectMembership" parameterType="map" resultType="int">
	    SELECT COUNT(*)
	    FROM members
	    WHERE PROJECT_ID = #{projectId}
	      AND USER_CODE = #{userCode}
	</select>




</mapper>